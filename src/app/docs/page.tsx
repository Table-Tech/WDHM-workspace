import { DocsPage } from '@/components/docs';

export const metadata = {
  title: 'Knowledge Hub - TechTable',
  description: 'Interne documentatie, handleidingen, klantinformatie en processen.',
};

export default function DocsRoute() {
  return <DocsPage />;
}
